import {PasswordMatchDirective } from './passsword-match.directive';

describe('PassswordMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatchDirective();
    expect(directive).toBeTruthy();
  });
});
